<Window x:Class="DesktopNotifier.Views.NotificationToastWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="DesktopNotifier"
        Height="110"
        Width="300"
        WindowStyle="None"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        AllowsTransparency="True"
        Background="Transparent"
        Foreground="White"
        Topmost="True"
        WindowStartupLocation="Manual">
    <Border CornerRadius="8" Background="#FF2D2D30" Padding="12" Opacity="0.95">
        <StackPanel>
            <TextBlock Text="{Binding Topic}" FontWeight="Bold" FontSize="15"/>
            <TextBlock Text="{Binding Application}" FontStyle="Italic" Margin="0,4,0,0"/>
            <TextBlock Text="{Binding Text}" TextWrapping="Wrap" Margin="0,8,0,0"/>
        </StackPanel>
    </Border>
</Window>
