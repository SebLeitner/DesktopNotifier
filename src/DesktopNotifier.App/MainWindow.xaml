<Window x:Class="DesktopNotifier.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:converters="clr-namespace:DesktopNotifier.Converters"
        mc:Ignorable="d"
        Title="DesktopNotifier"
        Height="450"
        Width="800">
    <Window.Resources>
        <converters:BoolToStatusConverter x:Key="BoolToStatusConverter" />
    </Window.Resources>

    <DockPanel>
        <StackPanel Orientation="Horizontal" DockPanel.Dock="Top" Margin="10">
            <Button Content="Start" Width="120" Margin="0,0,10,0" Click="OnStart"/>
            <Button Content="Stop" Width="120" Click="OnStop"/>
            <TextBlock Margin="20,0,0,0" VerticalAlignment="Center">
                <Run Text="Status:"/> <Run Text=" "/>
                <Run Text="{Binding IsRunning, Converter={StaticResource BoolToStatusConverter}}"/>
            </TextBlock>
        </StackPanel>

        <ListView ItemsSource="{Binding Notifications}" Margin="10">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Zeit" Width="160" DisplayMemberBinding="{Binding CreatedAt}"/>
                    <GridViewColumn Header="Anwendung" Width="180" DisplayMemberBinding="{Binding Application}"/>
                    <GridViewColumn Header="Thema" Width="180" DisplayMemberBinding="{Binding Topic}"/>
                    <GridViewColumn Header="Text" Width="240" DisplayMemberBinding="{Binding Text}"/>
                </GridView>
            </ListView.View>
        </ListView>
    </DockPanel>
</Window>
